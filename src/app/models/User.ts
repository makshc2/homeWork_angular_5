export interface User {
  first_name: string;
  last_name: string;
  age?: number;
}

export interface Stack {
  stack: [{string: string}];
}

export interface Hobby {
  hobby: [{string: string}];
}

